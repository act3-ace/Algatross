epochs: 10
island_iterations: 2 # Number of iterations per epoch. Each triggers a pull from archive (new allies)
mainland_iterations: 0 # Number of iterations per epoch
checkpoint_folder: experiments/c53d9dd7
resume_epoch: null # use latest

log_dir: "experiments"
ray_config:
  log_to_driver: true

debug: false
rich_console: false
log_level: 20 # INFO
ckpt_interval_epochs: 1
visualize: false
